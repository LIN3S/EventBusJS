/*
 * This file is part of the EventBusJS library.
 *
 * Copyright (c) 2016-present LIN3S <info@lin3s.com>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 *
 * @author Beñat Espiña <benatespina@gmail.com>
 * @author Mikel Tuesta <mikeltuesta@gmail.com>
 */

'use strict';

import test from 'ava';
import 'babel-core/register';

test('can push an item into PriorityQueue', (t) => {
  // Test if priorityQueue.push is working as expected
});

test('can remove an item from PriorityQueue', (t) => {
  // Test if priorityQueue.remove is working as expected
});

test('can sort PriorityQueue items', (t) => {
  // Test if priority sorting is working as expected
});

test('can iterate through PriorityQueue items', (t) => {
  // Test if we can iterate through the items (for ... of)
});
